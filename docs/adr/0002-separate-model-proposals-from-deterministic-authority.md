# Separate model proposals from deterministic authority

Models may interpret language, identify uncertainty, and propose bounded typed selections. Deterministic code remains authoritative for facts, availability, safety status, money, dates, workflow transitions, permissions, fingerprints, and final rendering so that correctness-critical behaviour is inspectable, reproducible, and testable.
